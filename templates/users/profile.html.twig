{% extends 'base.html.twig' %}

{% block body %}

        <div class="mt-4 text-right">
            <a href="{{ path('users_edit') }}" class="btn btn-primary">
                {{ 'page.user.list.btn.edit' | trans }}
            </a>
        </div>

        {% for message in app.flashes('success') %}
            <div class="alert alert-success mt-4">{{ message | trans }}</div>
        {% endfor %}

    <div class="my-5">
        <h1>{{ 'page.user.profile.title' | trans }}</h1>

        <div class="row">
            <div class="col-xs-12 col-sm-12">Name : {{ user.name }}</div>
            <div class="col-xs-12 col-sm-12">Lastname : {{ user.lastname }}</div>
            <div class="col-xs-12 col-sm-12">Email : {{ user.email }}</div>
            <div class="col-xs-12 col-sm-12">Rôle(s) : {{ user.roles|join(', ')|lower }}</div>
        </div>
    </div>

{% endblock %}