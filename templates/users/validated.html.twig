{% extends 'base.html.twig' %}

{% block body %}
    <div class="jumbotron mt-3">
        <main role="main" class="inner cover text-center">
            <img src="{{ asset('/build/images/validate.png') }}" alt="Validate" class="w-25">
            <h1 class="cover-heading py-5">{{ message }}</h1>
            <p class="lead">
                <a href="{{ path('login') }}" class="btn btn-lg btn-primary">Se connecter</a>
            </p>
        </main>
    </div>
{% endblock %}