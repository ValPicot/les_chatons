{{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}
        {{ form_rest(form) }}
        <button type="submit" class="btn btn-success">{{ button|default('Enregistrer') }}</button>
{{ form_end(form) }}