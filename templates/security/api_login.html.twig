{% extends 'base.html.twig' %}

{% block body %}

    {{ form_start(form, {'action' : path('api_login'), 'attr' : {'class' : 'mt-4'}}) }}

    <div class="container">
        <div class="panel no-bottom">
            <div class="row">
                <div class="group half">
                    {{ form_row(form.username) }}
                </div>
                <div class="group half">
                    {{ form_row(form.password) }}
                </div>
            </div>

            {{ form_errors(form) }}
        </div>

        <button class="btn" type="submit">{{ 'form.login.sign_in' | trans  }}</button>
    </div>

    {{ form_end(form) }}

{#    <form action="{{ path('api_login') }}" method="post" class="mt-4">#}
{#        <div class="form-group">#}
{#            <label for="username">{{ 'form.login.username' | trans }}</label>#}
{#            <input type="text" id="username" name="_username" class="form-control" placeholder="{{ 'form.login.username' | trans }}" autofocus="">#}
{#        </div>#}
{#        <div class="form-group">#}
{#            <label for="password">{{ 'form.login.password' | trans }}</label>#}
{#            <input type="password" id="_password" name="_password" class="form-control mb-2" placeholder="{{ 'form.login.password' | trans }}">#}
{#        </div>#}
{#        <button type="submit" class="btn btn-primary">{{ 'form.login.sign_in' | trans  }}</button>#}
{#    </form>#}

{% endblock %}