{% extends 'base.html.twig' %}

{% block body %}

<h1 class="mt-3">Editer le chaton</h1>

    {{ form_start(form) }}
        {{ form_rest(form) }}
    <button type="submit" class="btn btn-success">Valider les modifications</button>
    {{ form_end(form) }}

{% endblock %}